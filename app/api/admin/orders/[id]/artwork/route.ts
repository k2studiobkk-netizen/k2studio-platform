import { env } from "cloudflare:workers";
import { getStaffUser } from "../../../../../staff-auth";

export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await getStaffUser())return new Response("Unauthorized",{status:401});const {id}=await params;const runtime=env as unknown as {DB:D1Database;ORDER_FILES:R2Bucket};const order=await runtime.DB.prepare("SELECT artwork_key, artwork_name, artwork_type FROM orders WHERE id = ? LIMIT 1").bind(Number(id)).first<{artwork_key:string;artwork_name:string;artwork_type:string}>();if(!order?.artwork_key)return new Response("Not found",{status:404});const object=await runtime.ORDER_FILES.get(order.artwork_key);if(!object)return new Response("Not found",{status:404});return new Response(object.body,{headers:{"content-type":order.artwork_type||"application/octet-stream","content-disposition":`attachment; filename*=UTF-8''${encodeURIComponent(order.artwork_name||"artwork")}`,"cache-control":"private, no-store"}});}
